class Post {
    constructor(id, Title, Content, Date, Image) {
      this.id = id;
      this.Title = Title;
      this.Content = Content;
      this.Date = Date;
      this.Image = Image;
    }
  }
  
  export default Post;